
/**
 * This is just here to mimic @restart/hooks
 * It's a worthless hook, don't bother using it.
 */

import { useState } from 'react';

export default function useCallbackRef () {
  return useState(null);
}
